<?xml version="1.0" encoding="utf-8"?>
<!--
    Matured bond row — mirrors the three-row layout of item_holding.xml so the
    breakdown line on the right matches the active bond's "Realized · Unrealized ·
    Dividends" treatment.
      1. Ticker (left) ←→ realized P&L (right)
      2. Maturity kicker (single-line, ellipsized)
      3. Breakdown line (right-aligned mono 9sp, ink_mute)
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp"
    android:alpha="0.85"
    android:background="?attr/selectableItemBackground">

    <!-- Row 1: ticker | realized P&L -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:baselineAligned="true">

        <TextView
            android:id="@+id/ticker"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:singleLine="true"
            android:ellipsize="end"
            tools:text="UA4000187348 · UAH" />

        <TextView
            android:id="@+id/pnl"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceTitleSmall"
            android:textStyle="bold"
            tools:text="+150.00 UAH"
            tools:textColor="@color/pnl_positive" />
    </LinearLayout>

    <!-- Row 2: maturity kicker — matches active row's typeCurrency style -->
    <TextView
        android:id="@+id/maturityLine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.FinMon.Label.Small"
        android:textColor="@color/fm_ink_mute"
        android:textSize="10sp"
        android:layout_marginTop="2dp"
        android:singleLine="true"
        android:ellipsize="end"
        tools:text="Matured 2026-04-20" />

    <!-- Row 3: breakdown line (right-aligned mono, matches active row) -->
    <TextView
        android:id="@+id/principalLine"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.FinMon.Mono.Small"
        android:textSize="9sp"
        android:textColor="@color/fm_ink_mute"
        android:gravity="end"
        android:layout_marginTop="2dp"
        android:singleLine="true"
        android:ellipsize="end"
        tools:text="10,000 + 1,250 coupons − 10,100 invested" />

</LinearLayout>
